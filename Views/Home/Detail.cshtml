@{
 Layout = "_Layout";
}


<h2>@Model.GetName()</h2>
<h3>@Model.GetNumber()</h3>
<br>
<h3>@Model.GetAddress().GetAddress()</h3>
<h3>@Model.GetAddress().GetCity() @Model.GetAddress().GetState() @Model.GetAddress().GetZip()</h3>
<br><br>
<form action="/remove/@Model.GetId()" method="post">
  <button type="submit" class="btn btn-danger">Delete This Contact</button>
</form>
